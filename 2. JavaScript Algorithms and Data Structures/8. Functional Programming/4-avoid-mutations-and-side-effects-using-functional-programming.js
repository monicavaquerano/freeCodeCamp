// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
let incremented;
incremented = fixedValue + 1;
return incremented;
/* or return fixedValue + 1; */
  // Only change code above this line
}
